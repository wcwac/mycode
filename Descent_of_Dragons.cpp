#include <bits/stdc++.h>
#include <ext/rope>
using namespace __gnu_cxx;
using namespace std;

map<int, rope<int>> v;

int main()
{
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; ++i)
    {
        v[0].push_back(i);
    }
    while (m--){
        
    }

    return 0;
}