#include <bits/stdc++.h>
using namespace std;
int _[100010];

int dfs(int x)
{
    if (x == 1)
        return 0;
    if (_[x] != -1)
        return _[x];
    if (x =)
}

int main()
{
    int n;
    cin >> n;
    for (int i = 1; i <= n; ++i)
    {
    }

    return 0;
}