#include <bits/stdc++.h>
using namespace std;
const int N = 60;
long long a[N];
int main()
{
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; ++i)
    {
        cin >> a[i];
    }
    cin >> l >> r;
    return 0;
}