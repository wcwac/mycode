#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int a[N],last[N];
int f[N][11];
int main()
{
    cin.tie(0)->sync_with_stdio(false);
    int n, m, k;
    cin >> n >> m >> k;
    for (int i = 1; i <= n; ++i)
    {
        cin >> a[i];
    }
    for (int i = 1; i <= n; ++i)
    {
        
    }
    return 0;
}